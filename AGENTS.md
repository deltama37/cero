# AGENTS.md

Cero 言語（ブートストラップコンパイラ `ceroc`、Go 製）のリポジトリで作業するエージェント向けの指示書。
言語の方針とロードマップは [`docs/adr/0001-cero-language-initial-policy.md`](docs/adr/0001-cero-language-initial-policy.md) を正とする。

## リポジトリ構成

- `cmd/ceroc/`: CLI のエントリポイント
- `internal/`: コンパイラ本体。`lexer` → `parser`（`ast`）→ `typecheck`（`types`）→ `lower`（`ir`）→ `wasm` の順に処理し、`driver` がこれらをつなぎ、`cli` がコマンドを扱う。`diag` はコンパイルエラーの型
- `examples/`: サンプルの Cero プログラム
- `docs/adr/`: 方針決定の記録（ADR）
- `docs/design/`: 機能ごとの設計書（設計フェーズで作成する）

## 開発フロー: 設計と実装でモデルを分ける

機能追加や大きめの変更は、**設計を Claude Opus 5.5、実装を Grok 4.7** が担当する。
親エージェントは Opus 5.5 で起動し、実装だけを Grok 4.7 のサブエージェントに任せる。
この指示自体が、サブエージェントのモデル指定に関するユーザーからの明示的な依頼である。

小さな修正（typo、1〜2 行の変更、ドキュメントのみの変更）では、このフローを省略してよい。

### 1. 設計フェーズ（親エージェント / Opus 5.5）

- 設計書を `docs/design/<機能名>.md` に作成する（ファイル名はケバブケース）。
- 方針レベルの決定（言語仕様・ツールチェーンの選定など）は、設計書とは別に `docs/adr/NNNN-<タイトル>.md` の ADR として残す。
- 設計書には次を含める。
  - 目的と、対象外にすること
  - 関連する ADR
  - 変更・追加するパッケージとファイル
  - 公開する型・関数のシグネチャ（Go のコード）
  - 処理の流れ（疑似コード）
  - エラーケースと、その扱い方
  - テスト観点（テーブル駆動テストのケース一覧）
  - 完了条件
- 実装者が迷わず実装できる粒度まで具体化する。判断を実装者に委ねない。

### 2. 実装フェーズ（サブエージェント / Grok 4.7）

- `Task` サブエージェントを `model: grok-4.7-high` で起動する。
- プロンプトには、設計書のパス、対象ブランチ、完了条件（`make check` が通ること）を含める。
- サブエージェントには次を守らせる。
  - 設計書に書かれた範囲だけを実装する。設計と食い違う点や不明点があれば、推測で進めずに報告する。
  - 設計書を書き換えない。
- 実装が大きい場合は、設計書の単位ごとにサブエージェントを分けて順番に実行する。

### 3. レビューフェーズ（親エージェント / Opus 5.5）

- 設計書と実装の差分を突き合わせ、設計から外れた点、テストの不足、エラー処理の漏れを確認する。
- 問題があれば、指摘内容を添えて実装サブエージェントに修正を依頼する。設計の誤りだった場合は、先に設計書を直す。
- `make check` が通ることを自分でも確認してから、コミット・PR 作成に進む。

## コーディング規約（Go）

- 引数が 4 つ以上の関数は、引数ごとに改行して書く。
- エラーには文脈を付けてラップする（`fmt.Errorf("...: %w", err)` など）。
- テストはテーブル駆動で書き、各テストとサブテストで `t.Parallel()` を呼ぶ。テストケース間で状態を共有しない。
- Go 1.22 以降なので、ループ変数のコピー（`tt := tt`）は不要。
- 外部依存は必要になるまで追加しない。

## 検証コマンド

```bash
make check   # gofmt + go vet + go test（完了条件）
make build   # ./bin/ceroc を生成
./bin/ceroc help
./bin/ceroc run examples/fib.cero   # 55 が出力される
```

`internal/driver` と `internal/cli` の E2E テストは `wasmtime` を使う。`PATH` にない場合は skip されるので、E2E まで確認するときは wasmtime を入れておく。Cloud Agent 環境では `.cursor/environment.json` の `install` から `scripts/install-wasmtime.sh` が実行され、バージョン固定の wasmtime が `/usr/local/bin` に入る。
